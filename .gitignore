/vendor/
/.phpunit.cache/
composer.lock
/tmp/
/e2e/node_modules/
test-results/

# Local developer environment — holds absolute paths to plugin sources.
.env
skills/*/infra/*/.env

# Per-user plugin-docs cleanup config (author names, emails, domains — not committed).
# Generate with: skills/elgg-migrate/bin/setup-plugin-docs-config.sh
skills/elgg-migrate/config/*.local.json

# PHP engine lives inside the elgg-migrate skill (and is mirrored into
# every sibling skill by bin/gen-elgg-infra.sh). Ignore the transient
# bits that composer/phpunit drop in each copy.
skills/*/vendor/
skills/*/.phpunit.cache/
skills/*/composer.lock

# Temporary analysis / fleet-scan output (regenerated by scripts)
skills/elgg-migrate/tmp/

# Beads / Dolt files (local workflow state — not committed)
.beads/
.dolt/
*.db
.beads-credential-key

# OpenWolf context-management tool (local-only)
.wolf/
.claude/rules/openwolf.md
.claude/scheduled_tasks.lock

# Other AI assistant vendor dirs (not used — only Claude Code)
.adal/
.agents/
.augment/
.bob/
.claude/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
skills-lock.json
.playwright-mcp/
bin/bd-swarm.sh
bin/bd-swarm-watch.sh
.swarm-logs
.idea