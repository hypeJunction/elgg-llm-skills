# Clean, throwaway DB containers for the metadata (entity_guid, name) index benchmark.
# Ports are NOT published to the host to avoid clashing with other local databases;
# the runner execs the client inside each container. Data lives in tmpfs so every
# `up` starts from a pristine, empty server (no persisted state between runs).
name: elgg-idx-bench

x-common: &common
  restart: "no"
  tmpfs:
    - /var/lib/mysql
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_DATABASE: bench
  # A large buffer pool so the 1M-row dataset lives fully in memory: we are
  # measuring index access-path efficiency, not disk I/O.
  command: ["--innodb-buffer-pool-size=1G"]

services:
  # MySQL 5.7 is still the version deployed on the reference production site, and
  # its optimizer is the one that mis-plans the river listing (leads with an
  # entities scan + temp table + filesort). 8.0+ already plans the JOIN form more
  # sanely, so 5.7 is required to reproduce the production pathology.
  mysql57:
    <<: *common
    image: mysql:5.7
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-proot"]
      interval: 2s
      timeout: 5s
      retries: 30

  mysql80:
    <<: *common
    image: mysql:8.0
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-proot"]
      interval: 2s
      timeout: 5s
      retries: 30

  mysql84:
    <<: *common
    image: mysql:8.4
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-proot"]
      interval: 2s
      timeout: 5s
      retries: 30

  mariadb106:
    <<: *common
    image: mariadb:10.6
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 2s
      timeout: 5s
      retries: 30

  mariadb1011:
    <<: *common
    image: mariadb:10.11
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 2s
      timeout: 5s
      retries: 30
