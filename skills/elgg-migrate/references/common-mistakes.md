# Common mistakes and their fixes

Lookup table for the most common migration pitfalls. Each row is a mistake
observed across multiple plugin migrations and the fix that works. When
activation fails or tests regress, check here first — most failures in
practice are entries in this table.

## Common Mistakes

| Mistake | Fix |
|---------|-----|
| Closures in elgg-plugin.php | Use `ClassName::class . '::method' => []` or Bootstrap class |
| Plugin dir case mismatch | Dir must match composer.json `name` (usually lowercase) |
| Skipping Docker gate | Always validate — catches serialization, missing deps, type errors |
| Running 4.x rules on 2.x code | Migrate 2→3 first, commit, then 3→4 |
| Not installing plugin deps | Run `composer install -d <plugin>` before Docker test |
| `run_sql_script()` in activate.php | Removed in 4.x — use `elgg()->db->updateData()` with inline SQL |
| `elgg_register_menu_item()` in elgg-plugin.php | Can't go in config — use Bootstrap::init() or register hook |
| Auto-generated hooks use wrong format | Generator outputs `[Class, 'method']` — must be `Class::class . '::method' => []` |
| `elgg_extend_view()` not in elgg-plugin.php | Generator now extracts to `view_extensions` key — verify after running |
| Conditional hooks lost during migration | `elgg_is_active_plugin()` guards need Bootstrap class, not elgg-plugin.php |
| Old hook handler signatures | `($hook,$type,$return,$params)` → `(\Elgg\Hook $hook)` — rule 018 automates this |
| `get_data()` / `insert_data()` etc. | Removed in 4.x — use `elgg()->db->getData()`, `insertData()`, etc. |
| `Elgg\Database` type hints | Renamed to `Elgg\Application\Database` in 4.x — update `use` imports |
| `self::getInstance()` singleton pattern | Use DI container: `elgg()->{'service.key'}` per `elgg-services.php` |
| Tables not created on activation | Elgg 4.x does NOT run `activate.php` — it calls `Bootstrap::activate()` instead. Move all activate.php logic (table creation, schema setup) into `Bootstrap::activate()` method. Delete activate.php after migration. Use `catch (\Throwable $e)` not `catch (DatabaseException)` because Doctrine DBAL throws PDOException that Elgg doesn't wrap. Also fix TEXT columns: MySQL strict mode rejects `DEFAULT ''` on TEXT — remove the DEFAULT |
| Plugin activates but site 500s | Always test RENDER after activation — hooks fire on page load and can crash. Common cause: plugin registers `head`/`page` or `view_vars` hooks that query custom tables not yet created. Add try/catch around DB queries for custom tables as defense-in-depth |
| `elgg_trigger_event_results()` used in 4.x | That's Elgg 5.x only! In 4.x use `elgg_trigger_plugin_hook()` (deprecated but works) |
| `elgg_register_event_handler` for view/view_vars | In 4.x, `view` and `view_vars` are HOOKS not events — use `elgg_register_plugin_hook_handler()` |
| start.php still exists (even empty) | Elgg 4.x REJECTS plugins with ANY start.php file — delete it completely |
| `::SUBTYPE` constant in elgg-plugin.php | Triggers autoload before classes registered — use string literals (::class is fine) |
| `ElggEntity::save()` missing `: bool` return type | Elgg 4.x added return type hints — subclasses must match |
| `elgg_unregister_css()` in Bootstrap | Removed in 4.x — just delete the call (was defensive cleanup) |
| `elgg_register_js()` / `elgg_define_js()` in Bootstrap | Removed in 4.x — JS views under `views/default/js/` auto-discover as AMD modules |
| `elgg_register_simplecache_view()` in Bootstrap | Removed in 4.x — static `.js`/`.css` views auto-discovered by simplecache |
| `views.php` alias in `view_extensions` | When views.php is removed, update `view_extensions` to use actual view paths |
| Global Elgg functions in namespaced Bootstrap | Must use `\elgg_*()` prefix — namespace resolution fails without backslash |
| `elgg_register_admin_menu_item()` in Bootstrap | Removed in 4.x — admin views at `views/default/admin/` are auto-discovered |
| `elgg_set_plugin_setting()` in actions | Removed in 4.x — use `elgg_get_plugin_from_id('id')->setSetting()` |
| `validate_username()` in hooks/actions | Removed in 4.x — use `elgg()->accounts->assertValidUsername()` with try/catch |
| `forward(REFERRER)` in action files | Removed in 4.x — use `return elgg_ok_response()` or `return elgg_error_response()` |
| `forward()` in resource views | Removed in 4.x — use `throw new \Elgg\Exceptions\Http\EntityNotFoundException()` |
| `register_error()` / `system_message()` | Removed in 4.x — use `return elgg_error_response()` / `elgg_ok_response()` |
| `get_installed_translations()` | Removed in 4.x — use `elgg()->translator->getInstalledTranslations()` |
| `elgg_add_subscription()` | Removed in 4.x — use `$entity->addRelationship($user_guid, 'notify'.$method)` |
| Conditional view extensions in elgg-plugin.php | `elgg_is_active_plugin()` guards must go in Bootstrap::init(), not elgg-plugin.php |
| Symfony Response + exit in AJAX actions | Replace with `return elgg_ok_response($data)` / `elgg_error_response($msg, '', 422)` |
| `elgg_get_registered_tag_metadata_names()` | Removed in 4.x — use `elgg_get_config('registered_tag_metadata_names') ?? ['tags']` |
| `require(['jquery-ui'])` in JS | Elgg 4.x uses granular jQuery UI — use `require(['jquery-ui/widgets/sortable'])` etc. |
| XHR JSON echo + exit in action files | Replace manual `echo json_encode(); exit;` with `return elgg_ok_response($data)` — response system handles content negotiation |
| `views.php` removed but views key lost | When AST rule removes `views.php`, verify the `'views'` key in `elgg-plugin.php` preserves JS/CSS path mappings |
| Helper functions in start.php | Move to `lib/functions.php` and load via `Bootstrap::boot()` — don't put in elgg-plugin.php |
| `elgg_get_config('dbprefix')` in raw SQL | Removed in 4.x — use QueryBuilder `$qb->subquery()` or `elgg()->db->getTablePrefix()` |
| `elgg.action()` in JS | Removed in 4.x — use `elgg/Ajax` module: `var ajax = new Ajax(); ajax.action(...)` |
| `\Elgg\Hook` type hint introduced in 4.x | WRONG — `\Elgg\Hook` single-arg signatures work from 3.x onward. The 3→4 change is DECLARATIVE config, not the type hint |
| Monolithic menu handler function | Split into dedicated namespaced classes: `Menus\Site::register`, `Menus\OwnerBlock::registerUserItem`, etc. |
| `elgg_register_entity_type()` for search | Replaced by `'searchable' => true` in entity `'capabilities'` (4.x). Don't register separately |
| `likes:is_likable` hook for liking | Replaced by `'likable' => true` in entity `'capabilities'` (4.x). Don't register a hook for this |
| `elgg_register_notification_event()` still in Bootstrap | Use declarative `'notifications'` key in elgg-plugin.php (4.x) with handler classes |
| `elgg()->group_tools->register()` in Bootstrap | Use declarative `'group_tools'` key in elgg-plugin.php (4.x): `['blog' => []]` |
| Missing `'plugin'` key in elgg-plugin.php | Required in 4.x to replace manifest.xml: `['name' => 'Plugin Name', 'activate_on_install' => true]` |
| `blog_prepare_form_vars()` procedural function | In 5.x, use `'form:prepare:fields'` event with a `PrepareFields` class handler |
| Hardcoded URLs like `"blog/owner/$name"` | Use `elgg_generate_url('collection:object:blog:owner', ['username' => $name])` from 3.x+ |
| Old translation keys like `'blog:blogs'` | Convention changed in 3.x: `'collection:object:blog'`, `'item:object:blog'`, `'add:object:blog'` |
| Not adding route middleware in 5.x | Routes need `UserPageOwnerGatekeeper`, `PageOwnerGatekeeper`, `GroupPageOwnerGatekeeper` middleware |
| Keeping delete action for entities | Core handles generic entity deletion from 4.x — remove plugin-specific delete actions |
| Missing `'restorable' => true` capability | Add to entity capabilities in 6.x for trash/soft-delete support |
| Not using `require_once` for lib/functions.php | In 4.x, helper functions go in `lib/functions.php` loaded via `require_once(__DIR__ . '/lib/functions.php')` at top of elgg-plugin.php |
| Mixing hooks and events keys in 4.x | In 4.x, use `'hooks'` for hooks (view, view_vars, permissions, etc.) and `'events'` for events (create, update, delete). In 5.x, everything merges into `'events'` |
| `elgg_register_classes()` removed but classes/ directory still exists | The 2→3 `removed-functions` rule strips the calls but does NOT synthesize the PSR-4 autoload replacement. After running automated rules, manually add to `composer.json`: `"autoload": {"psr-4": {"VendorName\\PluginName\\": "classes/VendorName/PluginName/"}}`. If the plugin vendored a third-party library under `vendors/<Lib>/` that the old `elgg_register_classes` registered, also add that namespace to PSR-4. Run `composer dump-autoload -d <plugin>` (NOT `composer install` — `elgg/elgg` is a metadata declaration, not a runtime dep to install into the plugin). |
| 2.x→3.x plugin has stub `composer.json` without `elgg/elgg` require | The SKILL says "the plugin has no composer.json — generate it" but some 2.x plugins ship a stub `composer.json` that's just `name`/`type`/`description` with no `require.elgg/elgg`. **Augment, don't replace**: keep existing fields, add `require.elgg/elgg ^3.0`, `require.php >=7.0`, `require.composer/installers ~1.0`, and the PSR-4 `autoload` block. |
| `composer install -d <plugin>` fails with "elgg/elgg not satisfiable" | Plugin's `require.elgg/elgg` is a metadata declaration of *compatibility*, not a runtime dep. Don't try to install it inside the plugin — Elgg is the host. Use `composer dump-autoload -d <plugin>` instead, which generates `vendor/autoload.php` for the PSR-4 mappings without resolving deps. |
| 2.x plugin `isActive()` returns ElggRelationship object (not bool) | In 2.x, `\ElggPlugin::isActive()` returns the `active_plugin` relationship object when active, `false` when not. Test assertions must use `assertNotFalse()` not `assertTrue()`. From 3.x onward, it returns a real bool. |
| Plugin entity from `generateEntities()` left in disabled state | In Elgg 3.x test environments, when `_elgg_services()->plugins->generateEntities()` registers a fresh plugin entity for the first time, it can leave `enabled` blank/false even though `getError()` is empty and `activate()` returns silently `false`. Force the state in the test bootstrap: `if (!$p->isEnabled()) $p->enable(); if (!$p->isActive()) $p->activate();`. The matching DB row may need a direct `UPDATE elgg_entities SET enabled='yes' WHERE guid=...` if `$p->enable()` doesn't stick. |
| Elgg 3.x normalizes plugin id to lowercase but dir is still camelCase | A plugin at `mod/hypeFilestore/` (camelCase, valid in 3.x) is registered as `hypefilestore` (lowercase) by Elgg 3.x's plugin manager. Test code looking up `elgg_get_plugin_from_id('hypeFilestore')` returns `null`. Use the lowercase id, OR write a case-tolerant lookup: `elgg_get_plugin_from_id('hypefilestore') ?: elgg_get_plugin_from_id('hypeFilestore')`. The 3→4 migration enforces lowercase dir per Iron Law 6, eliminating the dual naming. |
| SecuritySweep flags third-party demo files (e.g. `vendors/cropper/examples/`) | Vendored libraries often ship demo/example files that contain unsanitized `echo` of user input or other patterns the security sweep flags as XSS. These are not reachable in production (the plugin doesn't include them in its routes). Add `vendors/*/examples/`, `vendors/*/demo/`, `vendors/*/test/` to the security sweep exclude list, OR `git rm` the demo dirs as part of the migration cleanup. |
| Smoke-test pattern for legacy 2.x plugins | When migrating an old 2.x plugin (Elgg 1.x era code) through 2→3, comprehensive behavior coverage is wasted work — the structure will change again in 3→4 and 4→5, and tests will be rewritten each time. Write **smoke tests** instead: assert the plugin loads (factory function exists, DI container instantiates, services resolve, hook handlers callable) — not behavior. This catches the regressions a structural transform actually introduces (autoload broken, namespace renamed, signature changed) without locking in tests for code that's about to be rewritten. |
| `docker cp` to bind-mounted dest writes to host bind source | If a Docker container has a bind mount like `/var/www/html/mod/hypeFilestore -> /home/user/.../plugins/hypeFilestore` and you `docker cp ./local/. <container>:/var/www/html/mod/hypeFilestore/`, Docker writes through to the host bind source — re-creating the directory there if you'd previously moved it. This silently re-populates "deleted" workspaces. Always check `docker inspect <container> --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{println}}{{end}}'` before doing `docker cp` into a long-running container. |
| `elgg_view_input()` removed in 4.x | Plugin settings or form views call `elgg_view_input('text', [...])`; in 4.x this throws `Call to undefined function`. Drop-in fix: rewrite to `elgg_view('input/<type>', $vars)` — same parameter contract, `'<type>'` is the same string ('text', 'select', 'plaintext', 'submit', 'hidden', 'tags', etc.). Sed pattern: `s/elgg_view_input('\([a-z_]*\)',/elgg_view('input\/\1',/g`. The 4.x helper `elgg_view_field()` exists too but takes a single `$params` array with `'#type'` — it's a different shape, not a 1:1 swap. |
| Custom plugin admin pages 404 in 4.x | In 3.x, `mod/<plugin>/views/default/admin/<plugin>/<page>.php` was auto-discovered when navigated via `/admin/<plugin>/<page>`. In 4.x **only built-in admin paths auto-discover**; plugin admin pages must be registered as named routes. The pattern is: declare each admin page as a route in `elgg-plugin.php` with `'middleware' => [\Elgg\Router\Middleware\AdminGatekeeper::class]`, point `'resource'` at a thin shim under `views/default/resources/admin/<plugin>/<page>.php` that wraps the existing admin view body with `elgg_view_layout('admin', ['title' => ..., 'content' => elgg_view('admin/<plugin>/<page>'), 'filter' => false])`. Symptom before fix: HTTP 404 on the admin path while `/admin/plugin_settings/<id>` (built-in route) still works. |
| `forward()` in route:rewrite hook context | The common 4.x action fix is `return elgg_redirect_response($url)`, but a `route:rewrite` hook handler runs before any action and isn't allowed to return a `ResponseBuilder` — the framework ignores the return. Use raw `header('Location: ' . elgg_normalize_url($url), true, 302); exit;` inside the hook handler. The hook return value is meaningful only for "rewrite the route", not "redirect the request". |
| AST rule leaves broken `switch ($event)` after hook→event signature rewrite | The `hook-callback-signatures-4x` rule rewrites `public static function onCreate($event, $type, $entity)` → `public static function onCreate(\Elgg\Event $event)` correctly, but **does not fix `switch ($event)` body usages**. After the rule runs you get `switch ($event) { case 'create': ... }` — switching on the Event *object* instead of the event *name*. Always grep for `switch ($event` after running the rule and rewrite to `switch ($event->getName())`. Same applies to `if ($event === 'create')` and any other comparison against the old `$event` string parameter. |
| `@playwright/test` version drift breaks chromium binary | `tests/playwright/package.json` with `"@playwright/test": "^1.49.0"` resolves on `npm install` to whatever the latest 1.x is (e.g. 1.59.1), but the elgg{3,4} `node` profile uses `mcr.microsoft.com/playwright:v1.49.0-noble` which only ships the chromium binary for 1.49.0. Tests fail with `Executable doesn't exist at /ms-playwright/chromium_headless_shell-...`. Pin **exactly** to the image version: `"@playwright/test": "1.49.0"`. Whenever the elgg-migrate skill bumps its `node` image version, every plugin's `package.json` must follow. |
| Stale `composer.lock` from prior major blocks the next major's install | After `composer install -d <plugin>` runs in the elgg{N} container during one migration step, the resulting `composer.lock` (which `.gitignore` hides from git but persists in the bind mount) pins constraints to the OLD major. The next step bumps `composer.json` (e.g. `composer/installers ~1.0` → `^2.0`) and the next `composer install -d <plugin>` aborts: "Required package 'composer/installers' is in the lock file as 'v1.12.0' but that does not satisfy your constraint '^2.0'". Always `rm composer.lock` before running `composer install -d <plugin>` in a fresh major-version container. |
| `elgg/elgg` in plugin `composer.json#require` pulls bower-asset transitive chain | The skill's composer table previously suggested adding `require.elgg/elgg`. Don't. `composer install -d <plugin>` then tries to resolve `elgg/elgg` and follows its `bower-asset/fontawesome ~5.6` transitive dep, which can only be satisfied via asset-packagist — a repo no plugin's composer.json declares. Result: `Your requirements could not be resolved`. The plugin is *always* installed by being placed under `mod/`, not by composer; the Elgg version constraint belongs in `manifest.xml` (3.x) or `extra.elgg-plugin` (4.x). |
| Cache directory ends up root-owned after `elgg-install.sh` | The skill's `infra/elgg{3,4}/elgg-install.sh` runs as root during container start and creates `/var/www/data/cache/...` owned by root. Apache then serves as `www-data` and every page hits `Phpfastcache::IOException: Path "/var/www/data/cache/fastcache/elgg_boot/Files" is not writable`. Workaround until the install script is fixed: `docker exec <container> sh -c 'chown -R www-data:www-data /var/www/data && chmod -R 755 /var/www/data'` after the install completes. |
| `ELGG_SITE_URL` mismatch breaks intra-Docker-network testing | The skill's `infra/elgg{3,4}/docker-compose.yml` sets `ELGG_SITE_URL=http://localhost:${ELGG_PORT}/` for host-port access. But Playwright runs from the `node` profile container and reaches Elgg via Docker DNS at `http://elgg`. Elgg rejects mismatched `Host:` headers with HTTP 400. Fix in your override: `environment: ELGG_SITE_URL: "http://elgg/"` so the configured wwwroot matches the in-network access path. Host-port access from your shell still works (Apache doesn't enforce host) but the rendered URLs will use `http://elgg/` — fine for testing. |
| `form.elgg-form-login` matches twice in the 4.x default theme | The login resource view renders the login form once in the page body and once in a (CSS-hidden) topbar widget. Playwright `locator('form.elgg-form-login')` matches both, triggers strict-mode violation, and `.fill()` on the first match times out because it's the hidden one. Use `.last()` (the body form) or scope by container. Same trap exists for any other UI Elgg renders in both topbar and main body. |
| Iron Law 4 has no escape hatch for 2.x starting points | The skill ships Docker envs for elgg3 and elgg4 only; there is no elgg2 environment. Strict pre-migration test execution against the *current* version is structurally impossible for a 2.x → 3.x step. Documented waiver: write the unit suite to be runnable in a plain `php:8.1-cli` container (autoload via a tiny bootstrap; stub `elgg_*` globals in a stubs file) so it executes against the current 2.x source without an Elgg core, and write the Playwright suite ahead of time but defer first execution to elgg3 post-migration. **Commit both suites on the source branch before the migration branch.** Note the waiver in the test commit message. |
| `\Elgg\Di\DiContainer` is abstract in 4.x; `setFactory()` removed | The `\Elgg\Di\DiContainer` base class became `abstract` in Elgg 4.x and now requires `public static function getDefinitionSources(): array`. The old imperative API (`setFactory($key, $callable)` in the constructor) is gone — Elgg internally moved to PHP-DI 6 declarative definitions. Plugins extending DiContainer crash with "must implement getDefinitionSources" then "Call to undefined method setFactory". **Fix:** rewrite the plugin's container as a plain PHP class (don't extend DiContainer at all) with a `__get` magic accessor that lazy-constructs services. Preserve the same outward interface (`hypeFoo()->config`, `->hooks`, etc.) so calling code doesn't change. |
| Hardcoded camelCase `PLUGIN_ID` constant breaks lookup in 4.x | A common 2.x/3.x pattern is `class Config { const PLUGIN_ID = 'hypeFoo'; }` then `elgg_get_plugin_from_id(self::PLUGIN_ID)`. In 4.x, plugin IDs are normalized to lowercase, so the camelCase lookup returns `null` and any code that constructs services from `Config::factory()` (which calls `new Config($plugin)` where `$plugin` is now null) hits a TypeError on the constructor's `ElggPlugin` type hint. **Fix:** lowercase the constant. For 3.x→4.x transitional builds, use a fallback: `elgg_get_plugin_from_id(self::PLUGIN_ID) ?: elgg_get_plugin_from_id('hypeFoo')`. |
| Broken neighbor plugin in `mod/` blocks elgg4 boot of unrelated plugins | Elgg 4.x scans every directory in `/var/www/html/mod/` during plugin entity generation. If ANY of them is missing `composer.json`, the entire boot fails with `\Elgg\Exceptions\Plugin\ComposerException: Missing composer.json for plugin ID X (guid Y)`. This breaks activation of unrelated plugins under test. **Workarounds:** (a) `DELETE FROM elgg_entity_relationships WHERE relationship='active_plugin' AND guid_one=Y` (force-deactivate via SQL using the guid from the error), (b) docker exec the container and stub a minimal composer.json in the broken plugin's dir, (c) move the broken plugin out of `mod/` for the duration of the test. The bigger fix is to not bind-mount unmigrated plugin dirs into the elgg4 container in the first place. |
| Neighbor plugin's `elgg-services.php` or `classes/` crashes during plugins_load and blocks every other plugin's tests | A worse variant of the above. Elgg 4.x triggers the `plugins_load` system event during `bootCore()`, which calls `ElggPlugin->register()` on every plugin in `mod/` (regardless of `active_plugin` state) and includes the plugin's `elgg-services.php`. If a neighbor's services file has unmigrated patterns — `\DI\object()` instead of `\DI\create()` (PHP-DI 5 → 6 rename), `use Elgg\Di\ServiceFacade;` (trait removed in 4.x), `Class 'hypeJunction\Plugin' not found` (4.x classname change) — the include throws and the **entire bootCore aborts**, so no test in any plugin's IntegrationTestCase suite can run. Symptoms: every test in `BaseTestCase::createApplication()` errors at `BootHandler::bootPlugins()` → `EventsService::triggerSequence('plugins_load', 'system')`. **DB-level deactivation does NOT fix this** because Elgg loads services files for inactive plugins too. **Workarounds:** (a) stub the broken services file in the container only: `echo '<?php return [];' > /var/www/html/mod/<broken>/elgg-services.php` — also stub any classes the plugin's other files import via `use ...;` (faster than fixing the broken plugin properly); (b) fix the broken plugin properly via its own migration (the right answer at fleet level); (c) rename the broken plugin's directory out of `mod/` — fails on bind mounts because the host source is busy. **The plugin under test is not at fault** — its own activate, render, and direct-call tests pass. The block is environmental. **Sequence the fleet so each plugin's neighbors are at least minimally compatible before running its test gate.** A `plugins_load`-safe baseline (every plugin's elgg-services.php parses, even if returning `[]`) is a precondition for any plugin to clear gate "Pre-migration tests adapted and passing on TARGET version". Track the precondition explicitly in beads. |
| Plugin Bootstrap that calls `elgg_register_css` / `elgg_register_external_view` in init() | Both functions were removed in Elgg 4.x. Bootstrap classes that call them get `Error: Call to undefined function elgg_register_css()` at activation time. **Fix:** delete the call. CSS and JS that need to be served from the plugin should live as views under `views/default/css/` and `views/default/js/` — Elgg's simplecache auto-discovers them. Files vendored under `vendors/` (not Elgg views) cannot be served via `elgg_register_css`; consumer plugins must reference them through their own view files. |
| Plugin needs third-party deps but `composer install -d <plugin>` fails on `elgg/elgg ^4.0` resolution | Per Iron Law (don't polyfill, don't bypass), the plugin's `require.elgg/elgg` is metadata. To install the plugin's own third-party deps without resolving elgg/elgg's transitive npm-asset / ckeditor / guzzle chain, use the **stub-then-restore** pattern: `cp composer.json /tmp/bak; cat > composer.json <<EOF { only the third-party deps you need } EOF; composer install -d <plugin>; cp /tmp/bak composer.json; composer dump-autoload -d <plugin>`. The lock file and vendor/ end up populated with just the third-party packages, and the original composer.json is restored. Don't commit the stub. (Long-term fix: separate metadata-only constraints from runtime deps in the elgg-migrate skill, so plugins can declare elgg/elgg compatibility without composer trying to install it.) |
| Tempted to polyfill a removed Elgg function | DON'T. Iron Law 11 forbids polyfills for removed APIs. Refactor every call site to the modern target-version API instead. Examples: `elgg_set_ignore_access(true) … elgg_set_ignore_access($prev)` set/restore pattern → `elgg_call(ELGG_IGNORE_ACCESS, function () use (...) { … })` closure pattern; `forward($url)` → `return elgg_redirect_response($url)` (in actions) or `header('Location: ' . elgg_normalize_url($url), true, 302); exit;` (in route hooks); `register_error()` / `system_message()` → `elgg_error_response()` / `elgg_ok_response()`; `elgg_set_plugin_setting()` → `elgg_get_plugin_from_id($id)->setSetting()`; `validate_username()` → `elgg()->accounts->assertValidUsername()` with try/catch. The whole point of the migration is that `grep <removed_func>` against the plugin source comes up empty after the step closes. If a refactor needs non-trivial closure scoping, that effort IS the migration — do it, don't shim around it. (Polymorphic guards on hook/event handler signatures are different — those are NOT polyfills; they keep a method callable from both `(\Elgg\Hook $hook)` and `($hook,$type,$return,$params)` shapes during the transition, and the legacy shape is the *call signature*, not a removed function.) |
| Core plugin dep fails to resolve in per-plugin docker stack | `elgg_get_plugin_from_id('search')` (or any other core plugin declared in `plugin.dependencies`) returns `null` in the per-plugin Elgg 4.x container. Cause: composer-installed Elgg ships bundled plugins under `vendor/elgg/elgg/mod/`, but the plugin loader only scans `/var/www/html/mod/`. With the bind-mount isolation invariant only the plugin under test lives in `mod/`. **Fix** (already in the elgg4 `elgg-install.sh` template): before `generateEntities()`, loop over `vendor/elgg/elgg/mod/*/` and `ln -s` each into `/var/www/html/mod/` unless the name already exists (bind mount always wins). Without this, any plugin with a core dep fails activation with `WARNING: dep plugin X not in mod/ — skipping` followed by `Requires plugin 'X' to be active`. |
| `ElggPlugin::setPriority('after', $id)` silently does nothing | Elgg 4.x `ElggPlugin::setPriority()` does **not** accept `'after'`/`'before'` verbs. Valid inputs: integer, `'+N'`, `'-N'`, `'first'`, `'last'`. Passing `'after'` returns the unchanged priority and activate() still fails with `Requires to be positioned after plugin 'X'`. **Fix** (already in the elgg4 `elgg-install.sh` template): call `$plugin->setPriority('last')` before `$plugin->activate()` so the plugin sits past every currently-loaded dep. Works for any number of deps as long as they're all activated first. |
| PHPUnit integration tests error with `Table 'elgg.c_i_elgg_config' doesn't exist` | `tests/phpunit.xml` is missing the `<php><env>…</env></php>` block that sets `ELGG_DB_PREFIX=elgg_` (plus host/name/user/pass). Without those env vars, `\Elgg\IntegrationTestCase` picks up a default `c_i_` prefix and queries nonexistent tables on the very first `bootCore()` call. Every test errors at `BootService::boot`. **Fix:** copy the `<php>` block from `skills/elgg-test-writer/SKILL.md` §phpunit.xml into the plugin's `tests/phpunit.xml`. The test-writer template has this block; older hand-authored phpunit.xml files don't. |
| Lightbox/colorbox CSS loads but click handlers never bind | An Elgg plugin ships a named-define AMD module (e.g. `define('elgg/lightbox', …)`) under `views/default/elgg/lightbox.js`, but the plugin's `Bootstrap::init()` doesn't call `elgg_require_js('elgg/lightbox')`. 2→3 and 3→4 rules strip the legacy `elgg_register_js` / `elgg_load_js` pair but don't synthesize the replacement. **Symptom**: CSS loads, `#colorbox` DOM element exists (because the module ran once on page init), but clicking bound selectors does nothing because the page-level require graph never includes the module, so `lightbox.bind(selector)` never executed. **Fix**: add `elgg_require_js('<module-name>')` to `Bootstrap::init()` for every AMD module the plugin expects to be auto-loaded. |
| `ELGG_SITE_URL` set to `http://localhost:<port>/` breaks Playwright CSS-sheet inspection | Per-plugin docker stack exposes Elgg on host at `localhost:<port>` but runs Playwright from a `node` service on the same Docker network reaching Elgg via `http://elgg`. When Elgg's `wwwroot` is `http://localhost:<port>/`, Elgg generates asset URLs like `http://localhost:<port>/cache/.../elgg.css` which the Playwright browser either can't fetch or treats as cross-origin — making `document.styleSheets[i].cssRules` throw SecurityError and the CSS-presence smoke test silently return false. **Fix** (align with entry 87 of this table): set `ELGG_SITE_URL: "http://elgg/"` in `docker/docker-compose.yml`. Host-port access from the dev's shell still works; the rendered wwwroot inside the page just uses the in-network hostname. |
| `register/menu:*` hook return value is a `MenuItems` collection, not an array | In Elgg 4.x, `$hook->getValue()` in a `register/menu:*` handler returns an `Elgg\Collections\Collection` subclass (`MenuItems`), NOT a plain array. Code that passed the old 4-arg signature's `$return` directly through `array_merge($return, $items)` fails: "Expected parameter 1 to be an array, object given". The automated 018 rule converts the signature but does NOT fix the body. **Fix:** replace `array_merge($return, $items)` with `$return->merge($items)` (the Collection API). If the return type can vary across versions add a guard: `is_array($return) ? array_merge($return, $items) : ($return->merge($items) ?: $return)`. **Grep:** `array_merge.*\$return` inside any handler registered under the `register/menu:` hook namespace. |
| Hook handler body uses bare `$type`, `$return`, or `$params` after signature rewrite | The rule `hook-callback-signatures-4x` rewrites `($hook, $type, $return, $params)` → `(\Elgg\Hook $hook)` but does NOT update the handler body. Any use of the old positional variables after the rewrite is an undefined variable (PHP 8 fatal on strict). Common case: `canWriteToContainer($user->guid, $type, $subtype)` — `$type` is now undefined. **Fix:** replace `$type` with `$hook->getType()`, `$return` with `$hook->getValue()`, `$params` with `$hook->getParam('<key>')`. **Grep after every rule run:** `grep -rn '\$type\b\|\$return\b\|\$params\b' <plugin>/classes/ <plugin>/lib/` and cross-check each hit against the function signature. |
| `ElggFile::open()` / `write()` before `save()` loses all bytes in Elgg 5.x | In Elgg 5.x `DiskFilestore` computes the storage path from the entity GUID. Before `save()` is called the GUID is 0, so `open('write')` creates (or opens) a file at path `…/0/0/0/filename`. Subsequent `write($data)` succeeds with a return value but bytes land in the wrong path; after `save()` assigns the real GUID the entity's accessor resolves a completely different path, and that file is empty. **Symptom:** image/file entities save successfully but reading them back via `getIconURL()` or `getFileContents()` returns nothing. **Fix:** always call `$file->save()` first, then `$file->open('write')` and `$file->write($data)`. This ordering is safe because `DiskFilestore` only resolves the final path at `open()` time, which now has a valid GUID. **Affected code pattern:** `$file = new ElggFile(); $file->setFilename(…); $file->open('write'); $file->write($data); $file->save();` — reverse `open`/`write`/`save` to `save`/`open`/`write`. |
| Namespaced plugin constant (e.g. `hypeJunction\Geo\PLUGIN_ID`) defined in `init()` instead of `load()` | Procedural lib files (`lib/functions.php`) are loaded at `Bootstrap::load()` time. If those files reference a namespaced constant that the Bootstrap defines in `init()`, every function call made during `load()` or `boot()` that references the constant hits "Undefined constant". **Fix:** move constant definitions to `Bootstrap::load()` (or define them at the top of `lib/functions.php` itself). In test bootstraps, define constants before calling `\Elgg\Application::loadCore()` and use the lowercase plugin ID (4.x requirement). |
| Git-tracked custom plugin's `lib/functions.php` helpers undefined when another plugin calls them | `composer.json autoload.files` only works for **composer-installed** plugins (in `vendor/`). Plugins living directly in a SITE repo's `mod/` (git-tracked customs, not composer-managed) are never processed by site-level composer, so their `autoload.files` directive is a no-op. When plugin B's `Bootstrap::init()` calls `pluginA_some_helper()` from custom A's `lib/functions.php`, PHP fatals with `Call to undefined function <NS>\pluginA_some_helper()`. **Fix:** `require_once __DIR__ . '/lib/functions.php';` at the TOP of plugin A's `elgg-plugin.php` (before the `return [...]`). Elgg's `generateEntities()` loads `elgg-plugin.php` very early — before any plugin's Bootstrap runs — so the global helpers exist as soon as plugin discovery completes. The pattern is fundamentally different from composer-installed plugins; document the choice in the plugin's ARCHITECTURE.md. |
| `elgg_ws_expose_function()` undefined on Elgg 5.x | Removed in Elgg 5.x — only `elgg_ws_unexpose_function()` survives. REST methods are now declared via the `'webservices'` key in `elgg-plugin.php` or as `\Elgg\WebServices\ApiMethods\*` classes. **Defensive guard for plugins not yet refactored:** wrap the legacy `require_once 'lib/users.php'` (or wherever the old `elgg_ws_expose_function(...)` calls live) with `if (!function_exists('elgg_ws_expose_function')) { return; }` so the plugin no-ops cleanly on 5.x+ instead of fatalling on every request. Full refactor (declarative webservices) is its own session. |
| Plugin superseded by core in a later major (e.g. `apiadmin`, `mrclay_combiner`) | Some 2.x plugins ARE replaced by core functionality in Elgg 4.x+. Examples: `apiadmin` (registers `api_key` entity subtype — conflicts with Elgg 4.x+ core `web_services` which registers the same subtype; Elgg refuses to activate both); `mrclay_combiner` (legacy non-AMD asset combiner — `elgg_unregister_js('jquery')` + `elgg_extend_view('elgg.js', …)` calls assume the pre-4.x asset system and actively interfere with core's simplecache + AMD/ESM pipeline). **These plugins cannot be migrated forward, only DROPPED.** Remove from `.plugin-order.txt` on every branch ≥ 4.x with a commit message documenting the supersession ("dropped: apiadmin — core web_services supersedes; both register 'api_key' subtype"). Keep the legacy code on the 2.x/3.x branches for historical reference. Document in the version-step migration notes that the plugin is intentionally not in the activation set. |
| `plugin.dependencies: {X: {position: after}}` declaration causes activation FAIL even when plugin-order has X first | Elgg's `position-after` check evaluates plugin priorities at activate-time. The bodyology site entrypoint calls `setPriority('last')` immediately before `activate()`, which puts each plugin at the highest-priority slot at activation time — but the `position-after` check has its own ordering semantics that don't always agree with "last priority". Symptom: `Requires to be positioned after plugin 'X'` even though X activated successfully one line earlier in the loop. **Fix:** if `.plugin-order.txt` already ensures X is processed before the dependent, drop the explicit `'position' => 'after'` declaration entirely — rely on the entrypoint's ordering instead. Keep the dep only when activating outside the ordered loop (e.g. via admin UI). |
| `add_translation()` undefined on Elgg 5.x | Removed in Elgg 5.x. Language files must use `return [...]` format from 5.x onward — and 4.x supports both, so the safer migration is to convert ALL language files to `return` format during the 3→4 step. **Symptom on 5.x:** every request 500s with `Call to undefined function add_translation()` because Elgg includes language files at boot regardless of plugin activation state — a single unconverted language file in `mod/<plugin>/languages/*.php` takes down the whole site. **Conversion:** replace `$english = array(…); add_translation('en', $english);` with `return [ … ];` — wrap the array contents in `return [...]` and delete the assignment + the `add_translation()` call. Sweep ALL language files on the 5.x branch with `grep -rln 'add_translation' mod/*/languages/`. |
| Rsync'ing migrated files between `migrate/elgg-N.x` and `migrate/elgg-(N+1).x` branches | This is a freelance anti-pattern that silently violates Iron Laws 1 + 7. Each migrate branch needs its own migration session with that version step's AST rules, its own `--verify --security` run, its own pre-migration tests adapted for that step, and its own gate checklist. Rsync'ing the 4.x migrated files onto the 5.x branch ("they're the same plugin, right?") skips: the 4→5 AST rules that catch hook-name → event-name renames; the leak detection that catches 5.x APIs accidentally used in the 4.x migration; the pre-migration tests adapted for 5.x; and the 17 acceptance gates for that branch. **The work is incomplete and the commit must be redone.** Symptom: the plugin "activates on both branches" but a later 5.x-specific breaking change (e.g. `\Elgg\Event` vs `\Elgg\Hook`) silently produces wrong behavior. |
| Migrating multiple major versions in one session ("did 4x and 5x together") | Violates Iron Law 1 (never skip a major) and Iron Law 7 (linear version knowledge). A migration session that ends with both `\Elgg\Hook` (4.x) AND `\Elgg\Event` (5.x) type hints in the same plugin's files is leaking knowledge between versions. The agent thinks "this is 4.x+" when there's no such thing — each major has its own API. **Fix:** one session does one version step. Complete 2→3 with gates and commit. Next session does 3→4. After that, 4→5. The intermediate state must boot, render, and pass tests *at that version* before advancing — even if the agent "knows" what comes next. |

